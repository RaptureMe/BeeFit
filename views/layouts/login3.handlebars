<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="{{styles}}">
</head>

<body>
    
    <header>
        <h2 class="logo">{{logo}}</h2>
        <nav class="navigation">
            <a href="{{homeLink}}">HOME</a>
            <a href="{{aboutLink}}">ABOUT</a>
            <a href="{{trackerLink}}">TRACKER</a>
            <a href="{{contactLink}}">CONTACT</a>
            <button class="btnLogin-popup">{{loginText}}</button>
        </nav>
    </header>

    <div class="wrapper">
        <span class="icon-close"><ion-icon name="close-outline"></ion-icon></span>

        <div class="form-box login">
            <h2>{{formTitle}}</h2>
            <form action="{{formAction}}">
                <div class="input-box">
                    <span class="icon">
                        <ion-icon name="mail-outline"></ion-icon>
                    </span>
                    <input type="email" required>
                    <label>{{emailLabel}}</label>
                </div>
                <div class="input-box">
                    <span class="icon"><ion-icon name="lock-closed-outline"></ion-icon>
                    </span>
                    <input type="password" required>
                    <label>{{passwordLabel}}</label>
                </div>
                <div class="remember-forgot">
                    <label><input type="checkbox"> 
                    {{rememberLabel}}</label>
                    <a href="#">{{forgotLink}}</a>
                </div>
                <button type="submit" class="btn">{{buttonText}}
                <!-- Possible Register Button -->
                </button>
                <!-- <div class="login-register">
                    <p>Don't have an account already? <a
                    href="#" 
                    class="register-link">Register</a>
                    </p> -->
                </div>
            </form>
        </div>

        <!-- Register Button (Doesn't work yet) -->
        <!-- <div class="form-box register">
            <h2>Registration</h2>
            <form action="#">
                <div class="input-box">
                    <span class="icon"><ion-icon name="person"></ion-icon></span>
                    <input type="text" required>
                    <label>Username</label>
                </div>
                <div class="input-box">
                    <span class="icon"><ion-icon name="lock-closed-outline"></ion-icon></span>
                    <input type="password" required>
                    <label>Password</label>
                </div>
                <div class="remember-forgot">
                    <label><input type="checkbox">
                    I am not a robot!</label>
                </div>
                <button type="submit" 
                class="btn">Register</button>
                <div class="login-register">
                    <p>I have an account <a 
                    href="#" class="login-link">Login</a></p>
                </div>
            </form> -->
        <!-- </div> -->
    

    <script src="{{script}}"></script>
    <script type="module" src="{{ioniconsEsm}}"></script>
    <script nomodule src="{{ionicons}}"></script>
</body>
</html>
